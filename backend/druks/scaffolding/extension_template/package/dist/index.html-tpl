<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{ name }}</title>
  </head>
  <body>
    <!-- Placeholder served at /app/{{ name }}. Point your frontend build's
         output (e.g. vite's outDir) at this dist/ directory to replace it. -->
    <h1>{{ name }}</h1>
  </body>
</html>
